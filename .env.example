# Backend (NestJS)
NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://leadflow:senha@localhost:5432/leadflow
JWT_SECRET=troque_por_um_valor_seguro

# Frontend (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3000

# AWS (usado apenas localmente para testar scripts, nunca commitar valores reais)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=
